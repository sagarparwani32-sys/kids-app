// Directory readme
This directory contains Kotlin packages and files following Clean Architecture. Placeholder structure:

- `app` - Entry point for activity and application-wide context.
- `data` - Data sources such as repositories, DAOs, and Room entity setups.
- `domain` - Use cases or core business logic.
- `ui` - Presentation layer (UI logic).